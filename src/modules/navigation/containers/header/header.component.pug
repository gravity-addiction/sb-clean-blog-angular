header.masthead(
    [style.background-image]='safeBackgroudImage'
)
    .overlay
    .container
        .row
            .col-xl-8.col-lg-10.mx-auto
                ng-container(
                    *ngIf='siteHeading; else postHeading'
                )
                    .site-heading
                        h1 {{heading}}
                        span.subheading {{subHeading}}
                        sb-find-profile-input(*ngIf='profileSearching;')

ng-template(#postHeading)
    .post-heading
        h1 {{heading}}
        h2.subheading {{subHeading}}
        span.meta {{meta}}

